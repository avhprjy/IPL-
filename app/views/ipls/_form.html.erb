<%= form_for(@ipl) do |f| %>
  <% if @ipl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ipl.errors.count, "error") %> prohibited this ipl from being saved:</h2>

      <ul>
      <% @ipl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_name %><br>
    <%= f.text_field :player_name %>
  </div>
  <div class="field">
    <%= f.label :player_country %><br>
    <%= f.select :player_country, @countries %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
